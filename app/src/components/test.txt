                    <div v-if="selectedTrainTimetable">
                    <h3>時刻表</h3>
                    <ul>
                        <li
                        v-for="row in selectedTrainTimetableDisplay"
                        :key="row.stationSame"
                        :class="{ 'past-station': row.isPastStation }"
                        >
                        <strong>{{ row.stationName }}</strong>

                        <span
                            v-if="row.timeAr"
                            :class="{ 'text-red': row.isDelayed }"
                        >
                            到着 {{ row.timeAr }}
                            <span v-if="row.isDelayed" class="text-grey text-caption">
                            （定刻 {{ row.rawTimeAr }}）
                            </span>
                        </span>

                        <span
                            v-if="row.timeDe"
                            :class="{ 'text-red': row.isDelayed }"
                        >
                            発車 {{ row.timeDe }}
                            <small v-if="row.isDelayed">
                            （{{ selectedTrain.delay / 60 }}分遅れ）
                            </small>
                        </span>
                        </li>
                    </ul>
                    </div>